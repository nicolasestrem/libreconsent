# Decision Log

Numbered, append-only. Status: `accepted` · `open` (still undecided) · `superseded (by D-xxx)`.

| ID | Date | Decision | Rationale | Status |
|----|------|----------|-----------|--------|
| D-001 | 2026-07-26 | Project name: **libreconsent** (repo `libreconsent`, npm scope `@libreconsent`). | Chosen from a vetted shortlist. npm name + scope free and GitHub org unclaimed as of 2026-07-26; no consent-industry product found under the name. Candidates Consentio/Konsento/ConsentKit rejected (existing-company collisions). "Libre" signals free-as-in-freedom, matching the project's positioning. | accepted |
| D-002 | 2026-07-26 | License **MIT**. Alternative: AGPL-3.0 to deter closed-SaaS freeriding. | MIT is the category convention (cookieconsent, tarteaucitron); maximizes adoption. Settled before the first release candidate and encoded in every package, source file, and generated JavaScript artifact in Phase 9. | accepted |
| D-003 | 2026-07-26 | **No TCF, permanently** — no TC string emission, no `__tcfapi` provider, no CMP ID. Read-only `__tcfapi` consumption allowed in bridge only. | IAB registration ~€1,500/yr + Google annual recertification + CMP-ID liability for all forks = structurally incompatible with free FOSS (01 §1). | accepted |
| D-004 | 2026-07-26 | EEA/UK/CH-audience AdSense domains use **Google Privacy & messaging** as the sole banner; libreconsent runs there in bridge mode only. | Free, Google-certified, TCF v2.3-ready, drives Consent Mode incl. analytics_storage. Only zero-cost compliant option (01 §2, §4). | accepted |
| D-005 | 2026-07-26 | Google scope = **Consent Mode v2** (GA4, Google Ads tags, GTM). Chosen over "everything Google" after cost analysis. | Scoping decision of 2026-07-26; ad serving covered per D-004. | accepted |
| D-006 | 2026-07-26 | **EEA-wide GDPR/ePrivacy baseline** (equal-prominence reject, prior blocking, etc.); member-state-specific extras (e.g., CNIL guidance) treated as good practice, not requirements. | Any EEA visitor triggers GDPR; Google's rule is contractual and regulator-independent (01 §3). | accepted |
| D-007 | 2026-07-26 | Architecture: pnpm monorepo — `@libreconsent/core` / `ui` / `bridge` / `worker-log`; TypeScript strict; tsup ESM+IIFE; Vitest + Playwright; size-limit CI gates. | Standard, well-supported toolchain suited to AI-assisted implementation; size budgets enforce the lightweight goal. | accepted |
| D-008 | 2026-07-26 | **Zero runtime dependencies** in core/ui/bridge; Shadow DOM UI; CSS-custom-property theming; no external assets. | Embeddability, supply-chain safety, size budgets (03 §10). | accepted |
| D-009 | 2026-07-26 | Consent mode default strategy configurable: `denied-everywhere` (default) or `deniedRegions` list. | Safest default; region list serves mixed-audience portfolios (CFG-3). | accepted |
| D-010 | 2026-07-26 | **No built-in geo-IP**; pluggable `resolveRegion` with documented Cloudflare header pattern; strictest behavior when unresolved. | Keeps library network-silent (NFR-2/3); geo is deployment-specific (CFG-9). | accepted |
| D-011 | 2026-07-26 | US module is **opt-out** model with GPC auto-honor; no blocking wall in US regions. | Matches US state law model; GPC increasingly enforced (03 §7). | accepted |
| D-012 | 2026-07-26 | Consent receipts backend is **optional** (CF Worker + D1); core never depends on it; no IP/UA stored. | Keeps €0 + privacy promises; receipts are an audit aid, not a requirement (03 §9). | accepted |
| D-013 | 2026-07-26 | Default consent-mode deployment stance in docs: **basic** (tags gated), advanced opt-in. | EU DPA skepticism of pre-consent pings (07 KG-7). | accepted |
| D-014 | 2026-07-26 | Locales at launch: **EN built-in + FR reference dictionary**. | FR is retained for v1 to exercise the i18n layer from day one; release documentation makes clear that both engineering-written dictionaries require site-owner review. | accepted |
| D-015 | 2026-07-26 | Specs-first execution: **one phase = one PR**, traceability-gated (03 §12). | Reviewability; keeps each change reviewable in isolation; CI as arbiter. | accepted |
| D-016 | 2026-07-26 | Phase 1 public contract: services are nested under categories but have globally unique IDs; `CookieTableRow` has `name`, i18n-key `purpose`, and optional i18n-key `provider` / `duration` / `type`; `setConsent` applies categories, then service overrides, then category recomputation. | Persisted services use a flat map, so global IDs avoid ambiguity. Explicit service overrides make partial preference updates deterministic. | accepted |
| D-017 | 2026-07-26 | `getConsent()` exposes active consent only. Replayable `ready` carries initialization reason and sanitized lower-revision prefill; `ready` / `consent` replay, `change` does not. Decision methods return `void` and queue in order until async initialization finishes. | Keeps stale consent from being mistaken for authorization while still supporting an immediately callable pre-DOM API and late event subscribers. | accepted |
| D-018 | 2026-07-26 | Phase 1 persistence uses the encoded cookie as authority and localStorage as mirror/fallback. No writes occur before a decision; after a valid prior decision, reconciliation may repair one store. Normal changes and withdrawal preserve consent ID / creation time; expiry or revision renewal starts a new UUID lifecycle. | A cookie-first rule gives deterministic cross-store recovery. Lifecycle continuity supports auditability, while renewal clearly records a fresh decision. | accepted |
| D-019 | 2026-07-26 | CM-4 is fulfilled in Phase 2 by documenting both deployment models and their tradeoffs. The executable basic-mode BLK gate and its pre-consent network-silence guarantee are Phase 3 work. | This makes CM-4's phase boundary explicit: Phase 2 delivers Consent Mode defaults, updates, and operator guidance; Phase 3 delivers declarative tag blocking. It preserves the required basic and advanced deployments without representing the Phase 3 gate as already shipped. | accepted |
| D-020 | 2026-07-26 | Blocking is configured by `blocking: { nonce?, reloadOnWithdraw? }` with no `enabled` flag; the declarative markup is the opt-in. | A page with no `data-cmp-category` element costs one `querySelectorAll` and nothing else, so a flag would only add config surface. Both values are JSON-serializable, so `sameConfig()` singleton comparison needs no special case (unlike `resolveRegion`, which is compared by function identity). | accepted |
| D-021 | 2026-07-26 | A gate naming a category or service that does not exist in the configuration stays blocked permanently, silently — no throw, no console output. | Fail closed: the alternative (throwing) lets stale markup break an entire page, and warning noise trains operators to ignore it. Scanning happens after config validation, so a genuine config error has already been reported with a typed path. Region-inapplicable services fail closed through the same rule. | accepted |
| D-022 | 2026-07-26 | Document order is enforced by a serialized promise chain: a `src` script without `async` sets `.async = false` and is awaited before the next gate runs; a script carrying `async` is inserted without being awaited; inline scripts run synchronously on insertion. | Script-inserted scripts are async by default, so order would otherwise depend on network timing. Awaiting non-async gates reproduces what the parser would have done. Scripts marked `async` running out of order is their declared semantics, so preserving that is correctness, not a gap. `error` resolves the chain as well as `load`, so one failing tag cannot stall the remainder. | accepted |
| D-023 | 2026-07-26 | On revocation, embeds are re-blocked (src removed, placeholder restored) while already-executed scripts are left untouched. `reloadOnWithdraw` (default `false`) reloads when an executed script's gate is revoked. | The spec (BLK-5) is explicit that scripts cannot be un-executed but silent on embeds; embeds are trivially reversible, so reversing them is the honest reading of a withdrawal. The reload cannot loop: after it, the denied script never executes. Default `false` because an unexpected reload is a worse surprise than a stale tag. | accepted |
| D-024 | 2026-07-26 | A gated element's own nonce wins over `blocking.nonce`, and the element's `nonce` IDL property is read before its attribute. | Per-element nonces exist precisely to be per-element. CSP nonce hiding blanks the content attribute after parsing, so `getAttribute("nonce")` alone silently loses the value and the re-created script would be refused by CSP. | accepted |
| D-025 | 2026-07-26 | The flagship `pre-consent-network-silence` E2E serves a vendored byte-identical copy of real `gtag.js` (`tests/vendor/gtag.js`, provenance recorded) via Playwright request interception, and keeps `navigator.sendBeacon` in place while proving interception observes it. | Spec §11.2 requires real `gtag.js`; vendoring keeps genuine Google code executing while removing CI dependence on network egress to Google and on Google-side changes. Analytics prefers `sendBeacon`, so a dedicated permanent test asserts interception observes beacons — otherwise "zero requests" could be satisfied by traffic the test never observed. Deleting `sendBeacon` was the original plan and was dropped once interception was proven to cover it: the deletion was unnecessary unrealism. | accepted |
| D-026 | 2026-07-26 | The BLK-2 CSP fixture declares its policy with `<meta http-equiv="Content-Security-Policy">` and omits the head-snippet marker. | The examples server has no response-header mechanism, and a meta policy exercises the same nonce enforcement in the browser. The injected head artifact carries no nonce, so including it would make the fixture fail for a reason unrelated to blocking. | accepted |
| D-027 | 2026-07-27 | Renderer options live in `mount(api, uiOptions)` in `@libreconsent/ui`, not in `CmpConfig`. | Presentation never affects the consent record, so it must not join the core's configuration identity (`sameConfig` singleton comparison) or its 8 KB budget, which had ~700 B of headroom. Keeping validation in the UI package also keeps the core free of DOM-shaped config it never reads. | accepted |
| D-028 | 2026-07-27 | The core gains `registerRenderer(renderer)`; `showPreferences()` / `hide()` forward to it and stay inert no-ops when none is registered. Renderer exceptions are swallowed. | UI-5 names `showPreferences()` as the re-entry mechanism and the core README already promised integrations could call it before the UI existed, so the intent had to become operative without the core learning to render. A registry beats new event names: `emit()` clones per event type and would need a non-state payload path. | accepted |
| D-029 | 2026-07-27 | `ReadyEvent` gains `region: string \| null`. | The modal must hide services whose `onlyRegions` exclude the visitor — the core forces those to denied, so rendering a toggle would offer a choice it cannot honor. Before a decision there was no other way to read the resolved region: `ConsentState.region` only exists once consent exists. | accepted |
| D-030 | 2026-07-27 | UI-only translation keys ship in `@libreconsent/ui` (`uiEn` / `uiFr`), not in the core's dictionaries. Effective dictionary = UI English, then UI locale reference, then the host's configured translations. | Keeps banner prose out of the core budget while leaving every string overridable per locale through the existing `i18n.translations`. The core still owns the four shared decision labels it already shipped. | accepted |
| D-031 | 2026-07-27 | Opening preferences hides the banner (`hidden`) instead of removing it; it is revealed again before focus is restored when the visitor backs out undecided. | Focus restore needs its target still connected to the document. Removing the banner detached the Customize button, so Escape left focus on `<body>` and the freshly built banner then stole it — the E2E focus-restore assertion caught exactly this. | accepted |
| D-032 | 2026-07-27 | The stylesheet's element reset is written as `:where(.lc-root) :where(...)`, giving it zero specificity. | The first version used `.lc-root button` (0,1,1), which outranked `.lc-btn--primary` (0,1,0) and left both decision buttons with no background or accent colour. Equal prominence still "passed" because the two buttons were equally unstyled, so the E2E now also asserts the accept button's background is not transparent. | accepted |
| D-033 | 2026-07-27 | Styles are applied through a constructable `CSSStyleSheet` via `adoptedStyleSheets`, falling back to a `<style>` element. | Constructable sheets are exempt from `style-src`, so a strict-CSP host needs no `'unsafe-inline'`. The fallback covers Safari below 16.4, which the spec's browser matrix still includes. | accepted |
| D-034 | 2026-07-27 | Escape closes the preferences layer but is inert on the first layer, including the `modal` layout. Focus is still trapped in a modal-layout banner. | UI-3 requires ESC of the dialogs it governs; the second layer honors it. The first layer asks for a decision and has no dismiss action, so Escape has nothing to trigger — and a dismissible consent request would leave an ambiguous state that is neither consent nor refusal. Reject all is one keypress away and equally prominent, so trapping focus corners nobody into consenting (UI-7). | accepted |
| D-035 | 2026-07-27 | `@libreconsent/core` resolves to `packages/core/src/index.ts` through a tsconfig `paths` mapping and a matching Vitest alias, rather than through the package `types`/`main` entry. | CI runs Typecheck, Lint and Unit tests before Build, and `dist/` is gitignored, so package-entry resolution made `packages/ui` fail on a clean checkout while passing on any machine that had built once. Resolving to source removes the ordering dependency entirely; the published artifacts still resolve normally for consumers. | accepted |
| D-036 | 2026-07-27 | BLK-4 patterns are plain case-sensitive substrings matched against the `src` as written, configured as `blocking.blocklist: [{ pattern, category, service? }]`. Not regular expressions. | A substring cannot backtrack pathologically on a hostile URL, stays JSON-serializable so `sameConfig()` singleton comparison needs no special case (as D-020 requires), and is smaller than RegExp compilation plus the escaping rules operators would have to learn. Hostname and path prefixes are all the net needs to express. | accepted |
| D-037 | 2026-07-27 | The net intercepts by patching the `src` setter and `setAttribute` on `HTMLScriptElement.prototype`, marking a matching script `type="text/plain"` plus `data-cmp-category` so it becomes an ordinary BLK-1 gate. The MutationObserver only *registers* those gates; it does not block. | The first implementation followed the industry fallback — detach the node and veto `beforescriptexecute` from the observer — and the E2E proved it ineffective: the blocklisted script still fetched and executed in Chromium. Per the HTML standard, inserting a `<script src>` prepares it and starts its fetch, and removing it does not cancel the execution that follows, because the element's node document is unchanged. Interception must therefore precede insertion, and the prototype is the only single-point entry. Marking the element inert instead of cloning it reuses `recreate()`, the ordered chain, and withdrawal semantics unchanged. | accepted |
| D-038 | 2026-07-27 | The guardrail scan reads `packages/{core,ui,bridge}/src`, not `dist/`, and carries an exported `EXCEPTIONS` allow-list that is empty at Phase 5. | CI runs unit tests before Build and `dist/` is gitignored (the P-021 failure mode), so a dist-based scan would pass vacuously on a clean checkout. With zero runtime dependencies and no build-time code injection, a source guarantee propagates to the artifact. Making a waiver an explicit reviewed diff — rather than a silently loosened regex — is what keeps LOG-4's future opt-in `fetch` from eroding the zero-network rule. | accepted |
| D-039 | 2026-07-27 | The layout-shift gate asserts an empty entry list rather than a CLS score under a threshold, and ships a companion test that displaces real content to prove the observer works. | Both consent surfaces are `position: fixed`, so the correct value is exactly zero and any entry is a real regression; a threshold would let one through silently. Asserting entries also names the element that moved. The companion test exists because `toEqual([])` is the classic vacuous assertion — it would pass equally well if the entry type were misspelled or unsupported. | accepted |
| D-040 | 2026-07-27 | A gated dynamic script holds its URL in `data-cmp-src` and never has a real `src` assigned. `recreate()` restores it on a grant. | Marking the element `type="text/plain"` alone is reversible: `s.src = url; s.type = "text/javascript"` before insertion resurrected the tag, and the element was still a registered gate so consent then ran it twice (P-026). An element with no source cannot fetch regardless of what is done to its type afterwards, and holding a pending URL in `data-cmp-src` is already the established convention for iframe gates (BLK-3). | accepted |
| D-041 | 2026-07-27 | The BLK-4 observer registers nothing until the first scan has run. | The observer also sees parser-inserted nodes, so registering during parsing put a gate parsed after `init()` ahead of one parsed before it, inverting BLK-1's document-order guarantee on any page that enabled the net (P-024). Nothing is lost: `apply()` does not run before the scan, and `scan()` finds every gate then in the document in one document-order query. An opt-in best-effort feature must not be able to weaken a stated guarantee. | accepted |
| D-046 | 2026-07-27 | Two spec sentences were amended rather than left to drift: CORE-3's `consent` event is now "the first *active* state" (restored, implied, or first user decision), and UI-7's no-pre-checked rule is scoped to opt-in flows. CFG-8, US-1 and US-3 gained the clarifications D-043..D-045 settle, and US-4's dead start URL was replaced with the live pages. | The implied grant is only coherent if the spec admits a third source of active consent; leaving CORE-3 saying "restored state or first active user decision" would have made the shipped behavior a silent contradiction that the next phase reads as a bug. UI-7 exists to stop dark patterns, and showing an opt-out visitor their actual granted state is the opposite of a dark pattern — but the rule's letter forbade it, so the letter had to name the flow it governs. The 404 was found by the US-4 research and recorded with its replacements so the next reader does not repeat the dead end. | accepted |
| D-045 | 2026-07-27 | `usPrivacy.regions` defaults to `["US"]` and matches **prefix-aware** on the `-` separator (configured `US` covers a resolved `US-CA`; configured `US-CA` matches only California; `USA` never matches `US`). A supplied empty array is a config error. This differs from `onlyRegions`, which stays exact-match. | The two lists answer different questions. `onlyRegions` is shipped service targeting, where an operator names exactly the regions a vendor may run in. `usPrivacy.regions` is jurisdiction membership, and the resolver's granularity is the deployment's choice, not the operator's: the documented Cloudflare `CF-IPCountry` pattern returns `US` while a state-level resolver returns `US-CA`, and both must satisfy the same configuration. Prefix matching also mirrors Google's most-specific-region precedence for consent defaults, which the project already relies on. An empty array is rejected because it would disable the module without saying so; omitting the key is how the default is accepted. | accepted |
| D-044 | 2026-07-27 | Global Privacy Control (US-1) is applied **in memory on every page load** — the implied grant minus the ad-mapped categories, `gpcApplied: true` — and **never persisted**. An active stored decision takes precedence: GPC applies only when none exists. | Re-deriving per load tracks the live signal: the visitor may switch GPC off, and a stored copy would keep opting them out afterwards. Not persisting also keeps CORE-8 literally true and means the GPC state can never masquerade as a decision the visitor made. Precedence for a stored decision is supported by CCPA § 7025(c)(3) and Colorado's rules, which let a consumer's specific later consent override the general signal — and in this architecture any stored grant was necessarily made through our own UI on a browser already sending GPC, so the consent post-dates the signal. GPC is honored again the moment that decision expires or is withdrawn. The § 7025(c)(3) conflict-notification nuance and the counsel-review recommendation are recorded in `specs/US_NOTES.md` §6. | accepted |
| D-043 | 2026-07-27 | An undecided visitor in a configured US region receives a synthesized **implied grant**: an in-memory `ConsentState` with `implied: true` granting every applicable optional category, emitted through the existing replayable `consent` event, never persisted. `ready.reason` is *not* extended; `ConsentState.implied` is the discriminator. A stored decision invalidated by expiry or a revision bump **seeds** the implied state from its sanitized choices instead of granting everything. | US-3 makes US regions opt-out, so gated tags must run and Consent Mode must read granted before any decision — which needs a real active state, not a special case threaded through every subscriber. Synthesizing one reuses the Consent Mode adapter, the blocking controller and the UI unchanged: the renderer already suppresses the banner whenever `ready.consent` is non-null, so no new event surface was needed. `reason` was left alone because it truthfully describes what storage held, and several consumers switch on it. Seeding from lapsed choices is the correctness point: without it, an explicit opt-out would silently revert to granted the day the cookie aged out. Nothing is written, so CORE-8 holds — though third-party tags the grant releases will set their own cookies, which is the opt-out regime working as designed and is stated in the README. | accepted |
| D-042 | 2026-07-27 | **NFR-1 re-baselined once, from measurement:** core 8 → **12 KB** gzip, core+ui 15 → **19 KB**. Bridge (4 KB) and head snippet (1.5 KB) unchanged. G-3 remains a hard CI failure and a further raise requires the same evidence plus a new entry. | The original figures were set in §10 at Phase 0, before any code existed, and Phase 5 measurement disproved them: core finished at 7.96 KB with **40 bytes** left, and the remaining spec'd scope still has to land — Phase 6 (US module: GPC, Do-Not-Sell, region coexistence, CFG-8) ~1–1.5 KB and Phase 8 (`receiptEndpoint`, LOG-4) ~0.3–0.6 KB, projecting ~9.5–10 KB. A 40-byte margin is a tripwire, not a budget: it had already begun distorting the work rather than constraining it (a correctness improvement was declined on byte grounds during Phase 5, and P-028 alone consumed a third of what remained). The two figures move **together** because they are not independent — the UI adds ~6.7 KB, so a 15 KB combined cap held core to ~8.3 KB on any site rendering a banner, making the core limit non-binding and the combined one the true gate; raising core alone would have bought nothing. 12 KB leaves ~20% margin over the projection, still fails on a genuine multi-KB regression, and preserves the positioning claim (mainstream CMPs ship 30–100 KB+). Splitting the US module into its own package behind a core hook (the D-027 / `registerRenderer()` precedent) remains worthwhile so EU-only sites do not download US logic — but as an architecture decision on merit, no longer one forced by byte pressure. | accepted |
| D-047 | 2026-07-27 | A bridge observation has its own truthful `BridgeConsentState`: `source`, mapped `categories`, `services`, `gdprApplies`, and `observedAt`. Direct TCF observations report an empty service record; fallback observations preserve the fallback's service record. The bridge never fabricates core consent IDs, decision timestamps, revisions, or persistence claims. | BR-2 defines only category-level purpose mapping, and a TCF callback cannot prove that a libreconsent service choice, consent-record identity, or persisted decision exists. Reusing `ConsentState` by filling those fields would turn an interoperability adapter into an inaccurate audit record. A structural read/events API keeps integrations familiar without claiming more than the observed source can support. | accepted |
| D-048 | 2026-07-27 | The shipped TCF mapping is deliberately strict: `analytics` requires purposes 1, 7, 8, 9 and 10; `marketing` requires purposes 1, 2, 3 and 4. Each uses `match: "all"`. A configured `purposeMapping` is a complete replacement, not a merge, and `necessary` is always true and cannot be remapped. | Strict aggregation fails closed when an activity depends on several purposes. Complete replacement avoids an operator believing an omitted default category was removed while an implicit default still remained active. `match: "any"` remains available for explicitly modeled categories, but broadening the shipped defaults would grant a category on only partial evidence. | accepted |
| D-049 | 2026-07-27 | BR-4 fallback is an injected factory returning a structural core-compatible read/events API. `@libreconsent/bridge` never imports core or UI at runtime; the factory is called only after CMP discovery times out, and bridge teardown unsubscribes forwarding without calling the fallback's destructive `reset()`. | Injection preserves G-2 and the 4 kB bridge budget, prevents full-mode code from entering bridge-only bundles, and gives the host ownership of its core configuration and UI mount. Delayed creation enforces one consent surface: an external CMP wins if found; otherwise full libreconsent is initialized after handoff. Not calling the fallback's reset prevents a read-only adapter teardown from deleting a real user decision. | accepted |
| D-050 | 2026-07-27 | A TCF provider establishes bridge readiness only when its `addEventListener` callback reports `success === true` before the absolute discovery deadline. A non-throwing provider call without a callback remains pending, and synchronous or asynchronous rejection resumes bounded discovery; callbacks arriving at or after the deadline cannot displace the `none` / fallback handoff. | Treating return from a queued stub as successful registration made the replayable, one-shot `ready` event irreversible before the external API had confirmed that a listener existed. A later asynchronous rejection could then neither resume fallback coherently nor retract `source: "tcf"`. Callback-confirmed readiness keeps the read/events contract truthful, while `cmpuishown` can still establish `ready` with null consent before a usable decision exists. | accepted |
| D-051 | 2026-07-27 | `receiptEndpoint` is the complete relative or absolute HTTP(S) target. Core sends only after persistence, with `consent` for the first explicit state, `change` for later explicit states, and `withdraw` for withdrawal; restored, revision-prefill, implied-US, and GPC states never send. | A complete target keeps routing under operator control. Restricting receipts to explicit persisted decisions prevents initialization or synthesized regulatory state from masquerading as a user action. | accepted |
| D-052 | 2026-07-27 | The Worker accepts only exact configured Origins whose host matches the payload, strict JSON at or below 16 KiB, and 30 requests/minute per `Origin + consentId`. Allowed-origin responses receive CORS; disallowed origins do not. | Exact origins prevent wildcard credential surfaces, host matching prevents a permitted site from writing a trail for another host, and the binding key resists abuse without reading or retaining IP addresses. Cloudflare's per-location, eventually consistent limiter is documented as abuse resistance rather than global accounting. | accepted |
| D-053 | 2026-07-27 | Receipt rows contain only internal ID, consent ID, host, revision, JSON categories, client timestamp, action, and server `received_at`. Bearer retrieval returns the documented fields plus `receivedAt`; no pagination in Phase 8. | This is the minimum useful record for an ascending decision trail. Excluding request metadata, service choices, and geo data makes IP/UA/fingerprint storage structurally impossible and keeps service-level receipts and dashboards out of scope. | accepted |
| D-054 | 2026-07-27 | Retention defaults to 395 days, accepts 1–3650, and daily UTC purge compares server `received_at` with `ScheduledController.scheduledTime`. | Server time cannot be extended by a hostile or incorrect client clock. Using the scheduled controller clock makes the production handler directly time-travel testable without exposing a purge endpoint. | accepted |
| D-055 | 2026-07-27 | The remote purge gate uses Wrangler's resource-level remote binding (`DB.remote: true`) with local Worker execution, not legacy whole-Worker `wrangler dev --remote`. | Wrangler 4.114 recommends remote bindings as the replacement. The gate output identifies D1 as `remote`, keeps the rate limiter and variables local, exercises the scheduled test route, and verifies through the deployed bearer endpoint that the same remote row was purged. This gives the test-only scheduled route and real D1 without deploying a purge route. | accepted |
| D-056 | 2026-07-27 | The v1 semver API is package-root ESM/types only. Core, UI, and bridge keep named IIFE files, and core keeps the synchronous head-snippet file, as stable self-hosted v1 artifacts without opening package subpath exports. | Root-only exports make accidental internal coupling fail fast while preserving the browser deployment model. The release audit installs real tarballs, runs package-root APIs and browser globals, and proves deep imports reject. | accepted |
| D-057 | 2026-07-27 | Phase 9 produces a verified `1.0.0` release candidate but does not publish npm packages. Registry availability and `@libreconsent` scope ownership are separate post-merge gates requiring explicit authorization. | A version and even a later repository tag do not prove registry availability. Keeping publication separate avoids an irreversible external write during release preparation. | accepted |
| D-058 | 2026-07-27 | Firefox and Playwright WebKit run focused smoke coverage for every release quickstart and the demo; Chromium retains the full suite. WebKit automation is not represented as exact Safari 15.4 hardware validation. | This gives useful cross-engine regression coverage without overstating what Playwright's WebKit build proves about the oldest supported Safari release on Apple hardware. | accepted |
| D-059 | 2026-07-28 | G-2 forbids third-party runtime dependencies. The sole allowed first-party package relationship is `@libreconsent/ui` requiring compatible `@libreconsent/core ^1.0.0` through peer metadata; core and bridge have no peers, and all three packages keep empty `dependencies`. | The UI's public declarations reference core types and its API consumes a core instance, so omitting the peer lets package managers install a type-incomplete or incompatible UI. An exact first-party peer makes the existing package boundary enforceable without bundling code, adding a third-party supply-chain dependency, or changing the dependency-free browser artifact. The guardrail and release audit enforce the complete allowlist rather than accepting arbitrary peers. | accepted |
| D-060 | 2026-07-29 | Copyable quickstarts carry a tracked mirror of their exact built browser artifacts under `examples/vendor/libreconsent/` and reference it only by relative URL. The Consent Mode bootstrap remains inline; `/api/region` remains an external deployment prerequisite that fails closed when absent. | Repository-only aliases, HTML rewriting, and endpoint emulation proved a copied page could appear to work while failing on a normal static host. Byte comparison catches drift; browser smoke coverage on a static server proves the shipped copy contract without changing the application fixtures that need their richer test server. | accepted |
| D-061 | 2026-07-29 | `pnpm build` must not synchronize tracked quickstart mirrors. Updating mirrors is the explicit `pnpm quickstarts:sync-assets` maintainer step; release audit rejects a build script that would synchronize and compares the resulting build with committed files before any sync can mask drift. Every local `src` or `href` in a quickstart must be relative rather than root-absolute. | A build side effect rewrote stale tracked mirrors before the audit compared them, making the byte-identity check tautological in CI. Separating generation from validation restores the intended stale-file failure, and auditing that separation prevents a future script edit from reopening the hole. A prefix-specific absolute-path ban still allowed `/vendor/...`, which breaks copied quickstarts under a path prefix; rejecting all local root paths makes the portability contract enforceable. | accepted |
| D-062 | 2026-07-29 | Repository workflows permit local actions but pin every remote action to a full immutable commit SHA; every checkout sets `persist-credentials: false`. The unused OpenCode workflow is removed. Claude retains read-only repository permissions and `id-token: write` solely for its action's OIDC authentication; no secret is removed in this repository change. | Mutable action references and persisted checkout tokens enlarge the supply-chain and credential surface. The OpenCode wrapper was unused and installed a moving `latest` action, so pinning could not make it deterministic. Claude has successful use, and its OIDC capability is required by its action without granting repository writes. Secret deletion is an external irreversible operation and remains separately authorized. | accepted |
| D-063 | 2026-07-29 | A Consent Mode mapping may use only the exact object `{ mode: 'fixed', value: 'denied' }` as an alternative to a category-ID string. Fixed entries never name a category, remain denied in every lifecycle and regional default, and are ignored by US/GPC category selection. Both runtime and synchronous head bootstrap validate this shape and fail closed. | Analytics-only sites need to grant measurement without ever making advertising consentable. A fixed policy object is clearer and safer than inventing an advertising category, while exact structural validation prevents a typo from becoming a permissive policy. This preserves every existing string mapping and keeps GPC focused on real categories. Google Consent Mode guidance was rechecked on 2026-07-29 (guide updated 2026-05-06). | accepted |
| D-064 | 2026-07-29 | Release preparation runs only from a clean detached approved SHA into an empty external directory. It builds once, preserves exactly four tarballs, records their SHA-256 and npm integrity plus browser-artifact hashes, and emits a fingerprinted JSON/Markdown approval record. Tagging, publication, registry verification, and GitHub Release creation are a separate, later step; the registry consumer gate runs outside every Git worktree. | Release preparation must be reproducible and reviewable without creating an irreversible tag, package version, or release. Binding the approval to every candidate field prevents a tarball, browser asset, release-note, or export change from silently inheriting approval. A separate external consumer proves the registry result rather than inferring it from local workspace links. | accepted |
| D-065 | 2026-07-29 | Release preparation and consumer evidence reject directories inside any Git worktree. The consumer gate pins both default and `@libreconsent` registry resolution to `https://registry.npmjs.org/` and verifies its installed lockfile records that public origin. | An external path outside the libreconsent checkout can still be another repository, where artifact writes would create unrelated changes. npm scope or default-registry configuration can direct a nominally public lookup to a private mirror; explicit registry configuration plus lockfile provenance makes the gate evidence about the public registry rather than an operator-local substitute. | accepted |
| D-066 | 2026-07-29 | Chromium is the only required browser automation. Firefox/WebKit portability, compatibility, CI installation, and consumer-gate loops are removed until a real user, reported defect, or support commitment justifies them. | The project has no released users, and the multi-engine matrix created slow, flaky failures without delivering proportionate value. Chromium retains a direct static-portability smoke; raw release checks and static-server tests retain the non-browser contract. | accepted |
| D-067 | 2026-07-29 | The persistent settings button (UI-5) becomes a 40x40 icon disc at reduced opacity whose label is revealed on hover or keyboard focus, positionable to either bottom corner through the logical `floatingButtonPosition`, offsettable through `--libreconsent-fab-inset`, and carrying a consent indicator distinguished by shape rather than colour. The label is positioned out of flow, the icon is drawn in CSS rather than SVG, and the state is exposed through `aria-label` rather than as text. | An always-on text pill occupies page chrome permanently and cannot be moved off a corner the site already uses, which pushed hosts toward `floatingButton: false` and removed the persistent entry point CORE-10 depends on. Out-of-flow is what preserves NFR-2: a disc that grew in place would displace its own start position wherever its pinned edge is the far one — `bottom-end` in LTR, `bottom-start` in RTL — and hover sets no `hadRecentInput`, so those shifts would reach field CLS unexcluded; it also sidesteps Safari 15.4's lack of grid-track interpolation. CSS over SVG because `el()` is typed to `HTMLElementTagNameMap` and cannot construct SVG at all, and a `createElementNS` helper plus path data cost roughly three times the bytes against the remaining NFR-1 headroom. `aria-label` over text keeps the visible label a prefix of the accessible name (WCAG 2.5.3) and keeps the state out of the DOM. Filled disc against hollow ring, with neither colour green nor red, keeps the indicator reporting rather than nudging (UI-7). Measured at core+ui 17.62 kB gzip against the 19 kB cap; no budget moves. | accepted |
