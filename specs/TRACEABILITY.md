# Traceability

Latest completed phase: 8

Advance this marker only after the phase definition of done and its tests pass.
`pnpm traceability` requires exactly one passing, non-empty row for every
requirement owned by each completed phase. Its verification evidence must name
a configured Vitest/Playwright test file or a supported named CI check.

| Requirement ID | Implementation file(s) | Test file(s) | Status |
|---|---|---|---|
| TOOL-1 | `package.json`; `pnpm-workspace.yaml`; `packages/core/package.json`; `packages/ui/package.json`; `packages/bridge/package.json`; `packages/worker-log/package.json` | `.github/workflows/ci.yml` — `Install dependencies`, `Build`; `packages/core/src/index.test.ts`; `packages/ui/src/index.test.ts`; `packages/bridge/src/index.test.ts`; `packages/worker-log/src/index.test.ts` | Passing |
| TOOL-2 | `tsconfig.json`; `packages/core/tsup.config.ts`; `packages/ui/tsup.config.ts`; `packages/bridge/tsup.config.ts`; `packages/worker-log/tsup.config.ts` | `.github/workflows/ci.yml` — `Typecheck`, `Build` | Passing |
| TOOL-3 | `vitest.config.ts`; `playwright.config.ts`; `biome.json`; `package.json` | `.github/workflows/ci.yml` — `Lint`, `Unit tests`, `Size budgets`, `E2E tests`, `Accessibility tests`; `tests/fixtures.e2e.spec.ts`; `tests/fixtures.a11y.spec.ts` | Passing |
| TOOL-4 | `.github/workflows/ci.yml`; `.github/workflows/claude.yml`; `package.json`; `scripts/check-traceability.mjs`; `scripts/check-workflows.mjs` | `.github/workflows/ci.yml` — `Workflow supply-chain guardrails`, `Traceability`, `All gates`; `scripts/check-traceability.test.mjs`; `scripts/check-workflows.test.mjs` | Passing |
| TOOL-5 | `packages/core/src/index.ts`; `packages/core/src/types.ts`; `packages/core/README.md`; `packages/ui/README.md`; `packages/bridge/README.md`; `packages/worker-log/README.md` | `.github/workflows/ci.yml` — `Typecheck`, `Lint`, `Build` | Passing |
| CFG-1 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`moves a configured necessary category first and forces its invariants`<br>`readonly categories default enabled while optional categories cannot start enabled` | Passing |
| CFG-2 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes cookie rows, storage, consent settings, and region lists`<br>`rejects duplicate category IDs and globally duplicate service IDs` | Passing |
| CFG-3 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes cookie rows, storage, consent settings, and region lists`<br>`validates every effective mapping target when Consent Mode is enabled`<br>`rejects unknown Consent Mode signal keys` | Passing |
| CFG-4 | `packages/core/src/config.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`normalizes cookie rows, storage, consent settings, and region lists`<br>`throws a typed synchronous error for $name` (`invalid expiry`) | Passing |
| CFG-5 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`throws a typed synchronous error for $name` (`invalid revision`) | Passing |
| CFG-6 | `packages/core/src/config.ts`; `packages/core/src/errors.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `throws a typed synchronous error for $name`<br>`rejects duplicate category IDs and globally duplicate service IDs`<br>`requires every referenced prose key in the default dictionary` | Passing |
| CFG-7 | `packages/core/src/config.ts`; `packages/core/src/dictionaries.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `ships EN and FR dictionaries and falls partial secondary locales back to default`<br>`requires every referenced prose key in the default dictionary` | Passing |
| CFG-8 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `defaults to a disabled module that respects GPC across US regions`<br>`normalizes regions and trims the Do Not Sell selector`<br>`rejects an invalid $name`<br>`rejects an empty region list rather than silently disabling itself`<br>`validates the signal mapping the opt-out reads even when Consent Mode is off`<br>`rejects an ad signal mapped to a permanently granted category` | Passing |
| CFG-9 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `awaits region resolution, normalizes it, and applies matching services`<br>`uses strict behavior for $name region resolution` | Passing |
| CORE-1 | `packages/core/src/index.ts`; `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `returns the same singleton for the same normalized configuration`<br>`includes resolver function identity in singleton compatibility`<br>`throws synchronously when a repeated init is materially different` | Passing |
| CORE-2 | `packages/core/src/index.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `queues pre-initialization decisions in call order`<br>`keeps Phase 1 UI intents DOM-safe and renderer-free`<br>`clears active state, listeners, and stored data and releases the singleton` | Passing |
| CORE-3 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `emits ready before first consent and change only for later decisions`<br>`emits ready before consent when restoring an active decision`<br>`replays ready and current consent to late subscribers but never change`<br>`isolates errors from late ready replay callbacks`<br>`isolates errors from late consent replay callbacks`<br>`isolates throwing listeners so queued decisions still initialize in FIFO order`<br>`delivers reentrant decisions to every listener in FIFO order` | Passing |
| CORE-4 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `keeps getConsent active-only and creates a UUIDv4 state on first decision`<br>`uses getRandomValues for UUIDv4 when crypto.randomUUID is unavailable`<br>`preserves consent ID and createdAt across changes and withdrawal`<br>`keeps decision timestamps valid when the wall clock moves backward` | Passing |
| CORE-5 | `packages/core/src/storage.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `persists a decision to cookie and localStorage and restores it round-trip`<br>`uses a valid cookie as source of truth and reconciles its mirror`<br>`recovers an absent or corrupt cookie from a valid mirror`<br>`recovers from inaccessible cookie storage and never crashes on unavailable storage`<br>`writes configured cookie attributes and adds Secure only for HTTPS`<br>`keeps decision timestamps valid when the wall clock moves backward` | Passing |
| CORE-6 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `acceptAll and rejectAll affect every optional choice but never necessary`<br>`preserves enabled readonly categories and rejects attempts to change them` | Passing |
| CORE-7 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `applies category changes before service overrides and recomputes categories`<br>`supports service-less category choices`<br>`awaits region resolution, normalizes it, and applies matching services` | Passing |
| CORE-8 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts` | `packages/core/src/index.test.ts` — `performs absolutely no storage write before a user decision` | Passing |
| CORE-9 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts` | `packages/core/src/index.test.ts` — `expires decisions from updatedAt and leaves them inactive`<br>`does not expire a recently updated decision based on an old createdAt` | Passing |
| CORE-10 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `preserves consent ID and createdAt across changes and withdrawal`<br>`persists withdrawal and emits change immediately`<br>`withdraw before active consent emits change rather than consent`<br>`keeps decisions after an initial withdrawal on the change channel` | Passing |
| CORE-11 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `makes lower-revision consent inactive and exposes sanitized prefill`<br>`retains service-less category prefill when revision renewal uses an empty selection`<br>`gives each ready listener and late replay a defensive prefill copy` | Passing |
| CM-1 | `packages/core/src/head-bootstrap.ts`; `packages/core/src/head-snippet.ts`; `scripts/serve-examples.mjs`; `examples/basic-site/index.html`; `examples/gtm-site/index.html` | `packages/core/src/head-bootstrap.test.ts`; `tests/fixtures.e2e.spec.ts` — `basic-site queues the compiled consent default before js and config`<br>`gtm-site queues every default before its Consent Initialization marker` | Passing |
| CM-2 | `packages/core/src/consent-mode.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `maps all four signals for first, changed, rejected, and withdrawn consent`<br>`signals restored and queued decisions with custom category mappings` | Passing |
| CM-3 | `packages/core/src/head-bootstrap.ts` | `packages/core/src/head-bootstrap.test.ts` — `queues regional denial, global grant, and configured settings in order` | Passing |
| CM-4 | `specs/03_MASTER_PRODUCTION_SPEC.md`; `packages/core/README.md` | `.github/workflows/ci.yml` — `Build`; `tests/fixtures.e2e.spec.ts` — `basic-site queues the compiled consent default before js and config` | Passing |
| CM-5 | `packages/core/README.md`; `examples/gtm-site/index.html` | `tests/fixtures.e2e.spec.ts` — `gtm-site queues every default before its Consent Initialization marker` | Passing |
| CM-6 | `packages/core/README.md`; `specs/05_BUILD_REVIEW.md` | `.github/workflows/ci.yml` — `All gates`; `packages/core/src/index.test.ts` — `requires a positive waitForUpdate ($s)` | Passing |
| BLK-1 | `packages/core/src/blocking.ts`; `packages/core/src/lifecycle.ts`; `examples/blocking-site/index.html`; `examples/basic-site/index.html` | `packages/core/src/blocking.test.ts` — `holds document order across an inline, src, inline sequence`<br>`advances the remaining order when a gated src script fails`<br>`inserts an async gated script without awaiting it`<br>`clears force-async and preserves defer for an ordered gated script`<br>`copies every non-CMP attribute and strips the CMP ones`<br>`promotes data-cmp-type to the replacement type`<br>`leaves gated scripts inert and writes nothing before a decision`<br>`defers the first scan until DOMContentLoaded while the document is loading`; `tests/blocking.e2e.spec.ts` — `gated scripts stay inert until consent (BLK-1)`<br>`consent executes gated scripts in document order (BLK-1)`; `tests/pre-consent-network-silence.e2e.spec.ts` — `basic-site contacts no Google endpoint before a decision`<br>`accepting loads real gtag.js, grants the update, and lets collects through`<br>`rejecting keeps the page silent and the loader unfetched` | Passing |
| BLK-2 | `packages/core/src/blocking.ts`; `packages/core/src/config.ts`; `packages/core/src/types.ts`; `examples/csp-site/index.html` | `packages/core/src/blocking.test.ts` — `applies the configured nonce to a script that has none`<br>`prefers the element's own nonce over the configured one`<br>`adds no nonce when neither the element nor the config supplies one`<br>`throws a typed synchronous error for $name`; `tests/blocking.e2e.spec.ts` — `re-created scripts carry a nonce and satisfy CSP (BLK-2)` | Passing |
| BLK-3 | `packages/core/src/blocking.ts`; `packages/core/src/i18n.ts`; `packages/core/src/dictionaries.ts`; `packages/core/README.md` | `packages/core/src/blocking.test.ts` — `blocks an iframe and renders a localized placeholder before it`<br>`reveals the iframe and restores the author's display on accept`<br>`gates and reveals a generic data-cmp-placeholder element`<br>`opens a service-scoped gate on the service, not its category`<br>`renders placeholder prose in the configured locale`<br>`fails closed for a gate naming an unknown category`; `tests/blocking.e2e.spec.ts` — `iframe embeds render an i18n placeholder and load only on consent (BLK-3)`<br>`generic placeholders hide their element until consent (BLK-3)` | Passing |
| BLK-5 | `packages/core/src/blocking.ts`; `packages/core/src/config.ts`; `packages/core/README.md` | `packages/core/src/blocking.test.ts` — `re-blocks embeds on withdrawal but leaves executed scripts in place`<br>`reloads when an executed script loses consent and the option is on`<br>`does not reload when nothing had executed`<br>`does not reload with the default reloadOnWithdraw`<br>`normalizes blocking defaults and freezes them`; `tests/blocking.e2e.spec.ts` — `withdrawal re-blocks embeds but cannot un-execute scripts (BLK-5)` | Passing |
| UI-1 | `packages/ui/src/banner.ts`; `packages/ui/src/styles.ts`; `packages/ui/src/options.ts`; `packages/ui/src/controller.ts` | `packages/ui/src/index.test.ts` — `renders the title, the short text, and all three decision actions`<br>`gives accept and reject identical markup so neither can be favored`<br>`renders the %s layout`; `packages/ui/src/options.test.ts` — `defaults every option when none are supplied`; `tests/ui.e2e.spec.ts` — `accept and reject are rendered with equal prominence (UI-1, UI-7)`<br>`keyboard alone completes the accept journey (UI-1, UI-3)`<br>`keyboard alone completes the reject journey (UI-1, UI-3)` | Passing |
| UI-2 | `packages/ui/src/modal.ts`; `packages/ui/src/context.ts`; `packages/ui/src/controller.ts` | `packages/ui/src/index.test.ts` — `renders one labelled toggle per service in a category`<br>`toggling a category checkbox sets every service it controls`<br>`drives the category checkbox from the services beneath it`<br>`renders one cookie row per configured cookie with translated prose`<br>`toggles the cookie disclosure panel and its aria-expanded state`<br>`saves the rendered controls as exactly one setConsent call`<br>`omits a service excluded by the resolved region`<br>`omits a category whose services are all excluded by the resolved region`; `tests/ui.e2e.spec.ts` — `keyboard alone saves a per-service choice in the second layer (UI-2, UI-3)`<br>`cookie tables disclose per service on demand (UI-2)` | Passing |
| UI-3 | `packages/ui/src/focus.ts`; `packages/ui/src/banner.ts`; `packages/ui/src/modal.ts`; `packages/ui/src/styles.ts`; `specs/A11Y_CHECKLIST.md` | `packages/ui/src/index.test.ts` — `labels and describes the dialog with elements that exist`<br>`moves focus into the dialog when the preferences layer opens`<br>`wraps Tab from the last focusable control back to the first`<br>`wraps Shift+Tab from the first focusable control back to the last`<br>`closes on Escape and restores focus to the element that opened it`<br>`releases the first layer's focus trap when the banner closes`<br>`releases the first layer's focus trap when preferences opened over it`; `tests/ui.a11y.spec.ts` — `the banner is accessible in the ${scheme} theme (UI-3, UI-4)`<br>`the preferences modal is accessible in the ${scheme} theme (UI-3, UI-4)`<br>`the persistent settings button is accessible (UI-3, UI-5)`<br>`reduced motion is honored on both layers (UI-3)`; `tests/ui.e2e.spec.ts` — `preferences trap focus, close on Escape, and restore focus (UI-3)`; `.github/workflows/ci.yml` — `Accessibility tests` | Passing |
| UI-4 | `packages/ui/src/styles.ts`; `packages/ui/src/controller.ts`; `packages/ui/src/options.ts` | `packages/ui/src/index.test.ts` — `applies a pinned theme to the rendered root (UI-4)`<br>`leaves the theme unpinned by default (UI-4)`; `tests/ui.e2e.spec.ts` — `the banner renders in a shadow root and honors theme tokens (UI-4, UI-6)`; `tests/ui.a11y.spec.ts` — `the banner is accessible in the ${scheme} theme (UI-3, UI-4)`<br>`the preferences modal is accessible in the ${scheme} theme (UI-3, UI-4)` | Passing |
| UI-5 | `packages/ui/src/controller.ts`; `packages/ui/src/index.ts`; `packages/core/src/lifecycle.ts`; `examples/basic-site/index.html` | `packages/ui/src/index.test.ts` — `shows the floating settings button only once a decision exists`<br>`suppresses the floating settings button when it is turned off`<br>`opens the preferences layer from the floating settings button`<br>`opens the preferences layer from a page element marked data-cmp-open`<br>`opens the preferences layer through the core showPreferences intent`<br>`hides every surface through the core hide intent`; `packages/core/src/index.test.ts` — `forwards UI intents to a registered renderer until it unregisters`<br>`replaces an earlier renderer and ignores its stale unregister`<br>`drops the renderer on reset`; `tests/ui.e2e.spec.ts` — `re-entry reopens preferences after a decision (UI-5)` | Passing |
| UI-6 | `packages/ui/src/controller.ts`; `packages/ui/src/options.ts` | `packages/ui/src/index.test.ts` — `renders inside a shadow root by default`<br>`renders into the light DOM when the shadow root is turned off`<br>`mounts into a configured container element`<br>`throws when the same consent API is mounted twice`<br>`removes every rendered element and stops reacting to the core on unmount`; `tests/ui.e2e.spec.ts` — `the banner renders in a shadow root and honors theme tokens (UI-4, UI-6)` | Passing |
| UI-7 | `packages/ui/src/banner.ts`; `packages/ui/src/modal.ts`; `packages/ui/src/styles.ts`; `packages/ui/src/controller.ts` | `packages/ui/src/index.test.ts` — `renders a readonly category as an always-on badge with no checkbox`<br>`leaves every optional control unchecked before a decision`<br>`pre-checks only the services carried by a revision prefill (UI-7)`<br>`keeps the first layer hidden when a stored decision is restored`<br>`writes nothing while the banner and preferences are on screen`; `tests/ui.e2e.spec.ts` — `the second layer pre-checks nothing optional (UI-7)`<br>`a restored decision shows no banner (UI-7)`<br>`accept and reject are rendered with equal prominence (UI-1, UI-7)` | Passing |
| UI-8 | `packages/ui/src/i18n.ts`; `packages/ui/src/dictionaries.ts`; `packages/ui/src/styles.ts`; `packages/ui/src/controller.ts` | `packages/ui/src/i18n.test.ts` — `prefers an explicit locale override over the browser languages`<br>`throws when the locale override is not a configured locale`<br>`ignores the browser languages when autoDetect is off`<br>`picks the first exactly matching browser language when autoDetect is on`<br>`falls a regional browser language back to its base language`<br>`falls back to the configured default when no browser language matches`<br>`lets a host translation override the renderer reference string`<br>`fills the renderer-only keys the core does not ship`<br>`resolves English renderer strings for a locale with no reference dictionary`<br>`returns the key itself when the dictionary has no entry`; `packages/ui/src/index.test.ts` — `sets the host lang attribute from the resolved locale (UI-8)`; `tests/ui.e2e.spec.ts` — `the rendered surface declares its language (UI-8)` | Passing |
| BLK-4 | `packages/core/src/blocking.ts`; `packages/core/src/config.ts`; `packages/core/src/types.ts`; `examples/dynamic-site/index.html`; `packages/core/README.md` | `packages/core/src/blocking.test.ts` — `marks a matching script inert before it can be fetched`<br>`intercepts a URL assigned through setAttribute`<br>`leaves a script whose URL matches no pattern alone`<br>`leaves a matching script alone while its category is granted`<br>`intercepts again once consent is withdrawn`<br>`never sees a dynamically injected inline script`<br>`maps a pattern to a service when one is configured`<br>`executes an intercepted script once its category is granted`<br>`registers a gate injected inside a container element`<br>`keeps a pattern naming an unknown category closed forever`<br>`leaves a script that already declares a category untouched`<br>`installs nothing when no pattern is configured`<br>`restores the patched script prototype on dispose`; `tests/blocking.e2e.spec.ts` — `a dynamically injected blocklisted script is never fetched or executed (BLK-4)`<br>`granting consent executes a neutered dynamic script (BLK-4)`<br>`a blocklisted script inside an injected container is neutered (BLK-4)`<br>`blocklisted scripts injected after withdrawal stay blocked (BLK-4)` | Passing |
| US-1 | `packages/core/src/lifecycle.ts`; `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `denies the mapped ad categories when Global Privacy Control is set (US-1)`<br>`follows the configured mapping when choosing what an opt-out denies`<br>`signals the opt-out to Consent Mode without a decision (US-1, CM-2)`<br>`leaves the signal unread when respectGPC is disabled`<br>`lets a stored decision outrank the signal (US-1)`; `tests/us-privacy.e2e.spec.ts` — `auto-applies the sale/share opt-out without a banner`<br>`keeps the gated ad script from ever being requested` | Passing |
| US-2 | `packages/ui/src/opt-out.ts`; `packages/ui/src/controller.ts`; `packages/ui/src/dictionaries.ts`; `packages/core/src/lifecycle.ts` | `packages/ui/src/index.test.ts` — `opens from a click inside the configured link (US-2)`<br>`records the opt-out as an explicit decision that keeps other consent`<br>`reports an opt-out already in force instead of offering it again`<br>`closes on Escape, restores focus, and stores nothing (UI-3, CORE-8)`<br>`survives a malformed selector without breaking other clicks`<br>`opens through the core renderer intent and the mount handle (US-2)`; `packages/core/src/index.test.ts` — `forwards the opt-out intent only to renderers that implement it (US-2)`; `tests/us-privacy.e2e.spec.ts` — `completes a keyboard-only opt-out that survives a reload`<br>`dismisses the dialog on Escape, restoring focus and storing nothing`; `tests/ui.a11y.spec.ts` — `the opt-out dialog is accessible in the light theme (UI-3, US-2)` | Passing |
| US-3 | `packages/core/src/lifecycle.ts`; `packages/core/src/config.ts`; `examples/us-only-site/index.html` | `packages/core/src/index.test.ts` — `treats an undecided US visitor as consenting without prompting or storing (US-3)`<br>`keeps region-restricted services denied under the implied grant`<br>`asks an opt-in decision for $name`<br>`matches a state-level region against a configured country`<br>`seeds the implied state from choices invalidated by $name`<br>`stays inert when the module is disabled`<br>`withdraws from an implied state into a persisted all-denied decision`; `packages/ui/src/index.test.ts` — `shows no banner and offers re-entry for an undecided US visitor (US-3)`; `tests/us-privacy.e2e.spec.ts` — `runs every gated script for an undecided visitor` | Passing |
| US-4 | `specs/US_NOTES.md`; `packages/core/README.md`; `packages/core/src/head-bootstrap.ts` | `.github/workflows/ci.yml` — `All gates`; `packages/core/src/head-bootstrap.test.ts` — `queues regional denial, global grant, and configured settings in order` | Passing |
| BR-1 | `packages/bridge/src/index.ts`; `packages/bridge/README.md`; `examples/bridge-site/index.html` | `packages/bridge/src/index.test.ts` — `rejects invalid config at its exact path`<br>`normalizes equivalent configs for singleton compatibility`<br>`removes a delayed CMP listener when synchronous ready resets the bridge`<br>`removes a queued CMP listener whose first callback arrives after reset`<br>`removes the listener through a CMP that replaced the queued stub`<br>`retains the CMP that confirmed the listener for teardown`<br>`removes the exact CMP listener during teardown`<br>`rejects and removes listener confirmation delivered after the deadline`<br>`ignores a non-numeric listener ID instead of removing the wrong listener`<br>`reset cancels polling and clears all bridge memory`<br>`cleans an earlier fallback subscription when a later one throws`<br>`ignores captured fallback callbacks invoked after reset`<br>`tears down every subscription when staged fallback replay resets`<br>`unsubscribes forwarding without calling the fallback reset`; `tests/bridge.e2e.spec.ts` — `adds no DOM, storage, Google signals, network, TC strings, or TCF assignment (BR-1)`<br>`teardown removes the external CMP listener without replacing __tcfapi (BR-1)`<br>`teardown removes a queued listener through the replacement CMP provider (BR-1)` | Passing |
| BR-2 | `packages/bridge/src/index.ts`; `packages/bridge/README.md`; `examples/bridge-site/index.html`; `scripts/serve-examples.mjs` | `packages/bridge/src/index.test.ts` — `ships a deeply frozen strict default mapping`<br>`supports complete replacement mappings with all and any aggregation`<br>`grants every mapped category when GDPR does not apply`<br>`polls with exponential backoff capped at 250 ms`<br>`discovers a CMP that appears after initialization`<br>`does not accept a silent queued stub without callback confirmation`<br>`does not accept a CMP discovered after a delayed polling deadline`<br>`keeps polling when CMP listener registration fails synchronously`<br>`keeps polling when CMP listener registration fails asynchronously`<br>`ignores unsuccessful callbacks after listener registration succeeds`<br>`discovers a replacement without registering a silent stub twice`<br>`activates fallback after synchronous registration failures reach the deadline`<br>`activates fallback after asynchronous registration rejection`<br>`rejects and removes listener confirmation delivered after the deadline`<br>`does not treat initial cmpuishown false values as a decision`<br>`ignores unsuccessful and malformed callbacks`<br>`removes a queued CMP listener whose first callback arrives after reset`<br>`removes the listener through a CMP that replaced the queued stub`<br>`ignores a non-numeric listener ID instead of removing the wrong listener`; `scripts/serve-examples.test.mjs` — `serves the compiled bridge artifact as executable JavaScript`; `tests/bridge.e2e.spec.ts` — `maps tcloaded consent, then emits change for useractioncomplete (BR-2, BR-3)`<br>`discovers a CMP installed after bridge initialization (BR-2)`<br>`treats initial cmpuishown values as transient rather than a decision (BR-2, BR-3)`<br>`teardown removes a queued listener through the replacement CMP provider (BR-1)` | Passing |
| BR-3 | `packages/bridge/src/index.ts`; `packages/bridge/README.md` | `packages/bridge/src/index.test.ts` — `emits ready before consent for an initial loaded state`<br>`does not accept a silent queued stub without callback confirmation`<br>`makes non-null fallback ready consent active before ready callbacks`<br>`suppresses duplicates and emits change only for later material state`<br>`replays ready and consent, never change, with defensive callback copies`<br>`isolates a throwing emitted callback from later callbacks`<br>`does not publish synchronous fallback replay before every subscription succeeds`<br>`discards staged fallback events when a subscription is not removable`<br>`ignores captured fallback callbacks invoked after reset`<br>`tears down every subscription when staged fallback replay resets`; `tests/bridge.e2e.spec.ts` — `maps tcloaded consent, then emits change for useractioncomplete (BR-2, BR-3)` | Passing |
| BR-4 | `packages/bridge/src/index.ts`; `packages/bridge/README.md`; `examples/bridge-site/index.html` | `packages/bridge/src/index.test.ts` — `emits one replayable none outcome at the deadline`<br>`does not accept a silent queued stub without callback confirmation`<br>`does not accept a CMP discovered after a delayed polling deadline`<br>`makes non-null fallback ready consent active before ready callbacks`<br>`proxies fallback ready, consent, change, and service choices`<br>`does not emit none before a fallback establishes ready`<br>`fails closed to none when the fallback factory throws`<br>`activates fallback after synchronous registration failures reach the deadline`<br>`activates fallback after asynchronous registration rejection`<br>`rejects and removes listener confirmation delivered after the deadline`<br>`cleans an earlier fallback subscription when a later one throws`<br>`does not publish synchronous fallback replay before every subscription succeeds`<br>`discards staged fallback events when a subscription is not removable`<br>`ignores captured fallback callbacks invoked after reset`<br>`tears down every subscription when staged fallback replay resets`; `tests/bridge.e2e.spec.ts` — `reports source none when no CMP appears before the deadline (BR-4)`<br>`hands off to real core/UI only after the timeout and renders one banner (BR-4)` | Passing |
| LOG-1 | `packages/worker-log/src/index.ts`; `packages/worker-log/migrations/0001_create_consent_receipts.sql`; `packages/worker-log/wrangler.example.jsonc`; `packages/worker-log/README.md`; `scripts/test-worker-log-remote.mjs`; `scripts/worker-log-remote-time.mjs` | `packages/worker-log/src/index.test.ts` — `D1 migration`<br>`POST /receipt`<br>`scheduled retention purge`; `scripts/worker-log-remote-time.test.mjs` — `advances strictly beyond every valid retention window` | Passing |
| LOG-2 | `packages/worker-log/src/index.ts`; `packages/worker-log/migrations/0001_create_consent_receipts.sql` | `packages/worker-log/src/index.test.ts` — `creates only the approved receipt columns and indexes`<br>`stores a valid receipt with server-controlled received_at`<br>`returns an ascending trail with only documented fields` | Passing |
| LOG-3 | `packages/worker-log/src/index.ts`; `packages/worker-log/README.md` | `packages/worker-log/src/index.test.ts` — `requires the configured bearer token`<br>`returns an empty trail for an unknown consent ID`<br>`returns an ascending trail with only documented fields` | Passing |
| LOG-4 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/receipt.ts`; `packages/core/src/types.ts`; `scripts/guardrails.test.mjs` | `packages/core/src/index.test.ts` — `optional consent receipt delivery (LOG-4)`<br>`configuration (CFG-1..7)`; `scripts/guardrails.test.mjs` — `core, ui, and bridge sources contain no prohibited construct` | Passing |
| NFR-1 | `.size-limit.cjs`; `packages/core/tsup.config.ts`; `packages/ui/tsup.config.ts`; `packages/bridge/tsup.config.ts` | `.github/workflows/ci.yml` — `Size budgets`, `All gates` | Passing |
| NFR-2 | `packages/ui/src/styles.ts`; `packages/ui/README.md` | `tests/hardening.e2e.spec.ts` — `banner and preferences injection shift no layout (NFR-2)`<br>`the layout-shift observer would catch a shift if one happened`; `tests/pre-consent-network-silence.e2e.spec.ts`; `scripts/guardrails.test.mjs` — `core, ui, and bridge sources contain no prohibited construct` | Passing |
| NFR-3 | `packages/core/package.json`; `packages/ui/package.json`; `packages/bridge/package.json`; `specs/SECURITY_CHECKLIST.md` | `scripts/guardrails.test.mjs` — `core, ui, and bridge declare no third-party runtime dependencies`<br>`core, ui, and bridge sources contain no prohibited construct`<br>`the scan actually inspects a meaningful number of files` | Passing |
| NFR-4 | `packages/core/src/blocking.ts`; `packages/core/src/storage.ts`; `examples/csp-site/index.html`; `specs/SECURITY_CHECKLIST.md` | `scripts/guardrails.test.mjs` — `the scanner detects a violation and ignores commented prose`; `tests/blocking.e2e.spec.ts` — `re-created scripts carry a nonce and satisfy CSP (BLK-2)`; `packages/core/src/blocking.test.ts` — `leaves gated scripts inert and writes nothing before a decision`<br>`restores the patched script prototype on dispose` | Passing |
| NFR-5 | `LICENSE`; `packages/core/LICENSE`; `packages/ui/LICENSE`; `packages/bridge/LICENSE`; `packages/worker-log/LICENSE`; `packages/core/tsup.config.ts`; `packages/ui/tsup.config.ts`; `packages/bridge/tsup.config.ts`; `packages/worker-log/tsup.config.ts`; `scripts/release-check.mjs` | `scripts/release-check.test.mjs` — `release package audit`; `.github/workflows/ci.yml` — `Release audit`, `Publication dry-runs`, `All gates` | Passing |
| NFR-6 | `packages/core/package.json`; `packages/ui/package.json`; `packages/bridge/package.json`; `packages/worker-log/package.json`; `scripts/npm-invocation.mjs`; `scripts/release-config.mjs`; `scripts/quickstart-assets.mjs`; `scripts/release-check.mjs`; `scripts/serve-static-quickstarts.mjs` | `scripts/release-check.test.mjs` — `release package audit`<br>`selects a portable npm invocation`<br>`requires exact vendored browser assets and relative quickstart paths`; `scripts/serve-static-quickstarts.test.mjs` — `ordinary static quickstart server`; `tests/quickstart-portability.e2e.spec.ts` — `ordinary static serving exposes no legacy artifact alias or API`<br>`uses copied relative browser assets`; `.github/workflows/ci.yml` — `All gates` | Passing |
