# Traceability

Latest completed phase: 2

Advance this marker only after the phase definition of done and its tests pass.
`pnpm traceability` requires exactly one passing, non-empty row for every
requirement owned by each completed phase. Its verification evidence must name
a configured Vitest/Playwright test file or a supported named CI check.

| Requirement ID | Implementation file(s) | Test file(s) | Status |
|---|---|---|---|
| TOOL-1 | `package.json`; `pnpm-workspace.yaml`; `packages/core/package.json`; `packages/ui/package.json`; `packages/bridge/package.json`; `packages/worker-log/package.json` | `.github/workflows/ci.yml` — `Install dependencies`, `Build`; `packages/core/src/index.test.ts`; `packages/ui/src/index.test.ts`; `packages/bridge/src/index.test.ts`; `packages/worker-log/src/index.test.ts` | Passing |
| TOOL-2 | `tsconfig.json`; `packages/core/tsup.config.ts`; `packages/ui/tsup.config.ts`; `packages/bridge/tsup.config.ts`; `packages/worker-log/tsup.config.ts` | `.github/workflows/ci.yml` — `Typecheck`, `Build` | Passing |
| TOOL-3 | `vitest.config.ts`; `playwright.config.ts`; `biome.json`; `package.json` | `.github/workflows/ci.yml` — `Lint`, `Unit tests`, `Size budgets`, `E2E tests`, `Accessibility tests`; `tests/fixtures.e2e.spec.ts`; `tests/fixtures.a11y.spec.ts` | Passing |
| TOOL-4 | `.github/workflows/ci.yml`; `package.json`; `scripts/check-traceability.mjs` | `.github/workflows/ci.yml` — `Traceability`, `All gates`; `scripts/check-traceability.test.mjs` | Passing |
| TOOL-5 | `packages/core/src/index.ts`; `packages/core/src/types.ts`; `packages/core/README.md`; `packages/ui/README.md`; `packages/bridge/README.md`; `packages/worker-log/README.md` | `.github/workflows/ci.yml` — `Typecheck`, `Lint`, `Build` | Passing |
| CFG-1 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`moves a configured necessary category first and forces its invariants`<br>`readonly categories default enabled while optional categories cannot start enabled` | Passing |
| CFG-2 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes cookie rows, storage, consent settings, and region lists`<br>`rejects duplicate category IDs and globally duplicate service IDs` | Passing |
| CFG-3 | `packages/core/src/config.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes cookie rows, storage, consent settings, and region lists`<br>`validates every effective mapping target when Consent Mode is enabled`<br>`rejects unknown Consent Mode signal keys` | Passing |
| CFG-4 | `packages/core/src/config.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`normalizes cookie rows, storage, consent settings, and region lists`<br>`throws a typed synchronous error for $name` (`invalid expiry`) | Passing |
| CFG-5 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `normalizes defaults, injects necessary first, and deeply freezes getConfig`<br>`throws a typed synchronous error for $name` (`invalid revision`) | Passing |
| CFG-6 | `packages/core/src/config.ts`; `packages/core/src/errors.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `throws a typed synchronous error for $name`<br>`rejects duplicate category IDs and globally duplicate service IDs`<br>`requires every referenced prose key in the default dictionary` | Passing |
| CFG-7 | `packages/core/src/config.ts`; `packages/core/src/dictionaries.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `ships EN and FR dictionaries and falls partial secondary locales back to default`<br>`requires every referenced prose key in the default dictionary` | Passing |
| CFG-9 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `awaits region resolution, normalizes it, and applies matching services`<br>`uses strict behavior for $name region resolution` | Passing |
| CORE-1 | `packages/core/src/index.ts`; `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `returns the same singleton for the same normalized configuration`<br>`includes resolver function identity in singleton compatibility`<br>`throws synchronously when a repeated init is materially different` | Passing |
| CORE-2 | `packages/core/src/index.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `queues pre-initialization decisions in call order`<br>`keeps Phase 1 UI intents DOM-safe and renderer-free`<br>`clears active state, listeners, and stored data and releases the singleton` | Passing |
| CORE-3 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `emits ready before first consent and change only for later decisions`<br>`emits ready before consent when restoring an active decision`<br>`replays ready and current consent to late subscribers but never change`<br>`isolates errors from late ready replay callbacks`<br>`isolates errors from late consent replay callbacks`<br>`isolates throwing listeners so queued decisions still initialize in FIFO order`<br>`delivers reentrant decisions to every listener in FIFO order` | Passing |
| CORE-4 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `keeps getConsent active-only and creates a UUIDv4 state on first decision`<br>`uses getRandomValues for UUIDv4 when crypto.randomUUID is unavailable`<br>`preserves consent ID and createdAt across changes and withdrawal`<br>`keeps decision timestamps valid when the wall clock moves backward` | Passing |
| CORE-5 | `packages/core/src/storage.ts`; `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `persists a decision to cookie and localStorage and restores it round-trip`<br>`uses a valid cookie as source of truth and reconciles its mirror`<br>`recovers an absent or corrupt cookie from a valid mirror`<br>`recovers from inaccessible cookie storage and never crashes on unavailable storage`<br>`writes configured cookie attributes and adds Secure only for HTTPS`<br>`keeps decision timestamps valid when the wall clock moves backward` | Passing |
| CORE-6 | `packages/core/src/config.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `acceptAll and rejectAll affect every optional choice but never necessary`<br>`preserves enabled readonly categories and rejects attempts to change them` | Passing |
| CORE-7 | `packages/core/src/lifecycle.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `applies category changes before service overrides and recomputes categories`<br>`supports service-less category choices`<br>`awaits region resolution, normalizes it, and applies matching services` | Passing |
| CORE-8 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts` | `packages/core/src/index.test.ts` — `performs absolutely no storage write before a user decision` | Passing |
| CORE-9 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts` | `packages/core/src/index.test.ts` — `expires decisions from updatedAt and leaves them inactive`<br>`does not expire a recently updated decision based on an old createdAt` | Passing |
| CORE-10 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `preserves consent ID and createdAt across changes and withdrawal`<br>`persists withdrawal and emits change immediately`<br>`withdraw before active consent emits change rather than consent`<br>`keeps decisions after an initial withdrawal on the change channel` | Passing |
| CORE-11 | `packages/core/src/lifecycle.ts`; `packages/core/src/storage.ts`; `packages/core/src/types.ts` | `packages/core/src/index.test.ts` — `makes lower-revision consent inactive and exposes sanitized prefill`<br>`retains service-less category prefill when revision renewal uses an empty selection`<br>`gives each ready listener and late replay a defensive prefill copy` | Passing |
| CM-1 | `packages/core/src/head-bootstrap.ts`; `packages/core/src/head-snippet.ts`; `scripts/serve-examples.mjs`; `examples/basic-site/index.html`; `examples/gtm-site/index.html` | `packages/core/src/head-bootstrap.test.ts`; `tests/fixtures.e2e.spec.ts` — `basic-site queues the compiled consent default before js and config`<br>`gtm-site queues every default before its Consent Initialization marker` | Passing |
| CM-2 | `packages/core/src/consent-mode.ts`; `packages/core/src/lifecycle.ts` | `packages/core/src/index.test.ts` — `maps all four signals for first, changed, rejected, and withdrawn consent`<br>`signals restored and queued decisions with custom category mappings` | Passing |
| CM-3 | `packages/core/src/head-bootstrap.ts` | `packages/core/src/head-bootstrap.test.ts` — `queues regional denial, global grant, and configured settings in order` | Passing |
| CM-4 | `specs/03_MASTER_PRODUCTION_SPEC.md`; `packages/core/README.md` | `.github/workflows/ci.yml` — `Build`; `tests/fixtures.e2e.spec.ts` — `basic-site queues the compiled consent default before js and config` | Passing |
| CM-5 | `packages/core/README.md`; `examples/gtm-site/index.html` | `tests/fixtures.e2e.spec.ts` — `gtm-site queues every default before its Consent Initialization marker` | Passing |
| CM-6 | `packages/core/README.md`; `specs/05_BUILD_REVIEW.md` | `.github/workflows/ci.yml` — `All gates`; `packages/core/src/index.test.ts` — `requires a positive waitForUpdate ($s)` | Passing |
